Future description of OptiHR

For clone the main repository and initialize the submodules do:
```bash
git submodule update --init --recursive
```
For update the submodule to the last version do:
```bash
git submodule update --remote
```
